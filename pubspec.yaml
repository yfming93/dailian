name: src
description:  flutter_app.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.0
  #  状态管理
  get: ^3.24.0

  # 屏幕适配
  flutter_screenutil: ^2.2.0

  # 权限请求
  permission_handler: ^5.0.1+1

#  #App版本信息
#  package_info: ^0.4.3+2

  #用来加载网络数据
  dio: 3.0.9

#  # WebView
#  webview_flutter: ^1.0.7

  #偏好设置
  shared_preferences: ^0.5.12+4

#  # 路径获取
#  path_provider: ^1.6.24

  #toast 消息提示小弹框
  fluttertoast: ^7.0.1+1

#  decimal: ^0.3.0
  common_utils: ^1.1.3
  flutter_easyrefresh: ^2.0.4
#  provide: ^1.0.2
#  web_socket_channel: ^1.0.15
#  #轮播图
#  flutter_swiper: ^1.1.6
  after_layout: ^1.0.7+2 # 在布局之后调用
  cached_network_image: ^2.2.0+1 # 缓存网络图片
  bot_toast: ^3.0.0 # toast
#  pull_to_refresh: ^1.6.3

#  image_cropper: ^1.3.1 # 图片裁剪
#  image_picker: ^0.6.7+21 # 图片选择器
#  qr_flutter: ^3.0.1 #二维码生成
#  r_scan: ^0.1.5 # 扫二维码
#   第三方分享
  share: ^0.6.5+4
  #城市选择
#  azlistview:  ^1.0.1

  #文字转拼音
#  lpinyin: ^1.0.8

#  drag_list: ^1.0.4


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true


  assets:
    - images/

